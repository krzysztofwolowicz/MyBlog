<%= link_to(" << Powrót do listy", posts_path, :class => 'back-link') %>

<div class="subjects delete">
  <h2>Usunięcie postu<h2>

  <%= form_for(@post, :method => 'delete') do |f| %>

    <p> Jesteś pewien, że chcesz usunąć ten post? </p>

    <p class="reference-name"><%= f.post.title %></p>

    <div class="form-buttons">
      <%= f.submit("Usuń Post") %>

    </div>
  <%end%>
</div>
